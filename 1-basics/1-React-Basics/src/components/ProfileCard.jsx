import React from "react";

const ProfileCard = () => {
  const styles = {
    padding: "15px",
    borderRadius: "8px",
    color: "black",
    backgroundColor: "lightgray",
  };
  return <div style={styles}>ProfileCard</div>;
};

export default ProfileCard;
