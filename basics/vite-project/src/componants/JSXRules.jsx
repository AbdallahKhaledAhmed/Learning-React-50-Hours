import React from "react";

const JSXRules = () => {
  return (
    <div className="JSXRules">
      <h1>JSX Rules</h1>
      <p>JSX must return only one single parent element</p>
      <p>JSX must be properly closed even if it's a self closing tag</p>
      <p>JSX uses cammal casing className not class</p>
    </div>
  );
};

export default JSXRules;
